let gameName = "spiderman";

gameName = "fifa"; // here value begin overwritten.

console.log(gameName);

const userName = "sushanth";

userName = "reddy";

console.log(userName); // will get an error.

// once const is declared it cannot be changed.

// const is used something more relaible, and we dont want it to be changed.